#!/bin/bash
# RETIRED (2026-07-20): the playbook/QA-portal lockstep process is stopped — commits
# no longer require a qa/playbook.json update. Manual test guidance lives in each
# feature's doc under docs/features/. To re-enable, restore this script from git
# history (it blocked feature-code commits that didn't touch qa/playbook.json).
exit 0
