{
  "name": "hr-platform",
  "version": "0.1.0",
  "private": true,
  "packageManager": "npm@10.8.2",
  "//": "HRMS & Payroll SaaS (India + New Zealand). Forked from Sitepresso; website/commerce verticals stripped. Workspaces: apps/* (platform, router, + hr-admin/ess as built) and packages/* (ui, admin-core, theme-engine, types).",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:backend": "node scripts/dev-local.js backend",
    "dev:platform": "node scripts/dev-local.js platform",
    "dev:router": "node scripts/dev-local.js router",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.3.0"
  }
}
