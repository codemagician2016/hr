import layoutPresets from '@hr/theme-engine/layout-presets.cjs';

export const {
  LAYOUT_PRESETS,
  AVAILABLE_VARIANTS,
  VARIANT_LABELS,
  SECTION_LABELS,
  DEFAULT_PRESET_KEY,
  resolvePreset,
  resolveVariant,
  resolveAllVariants,
} = layoutPresets;
