git_url("https://github.com/codemagician2016/drifthr-employee-certificates.git")
storage_mode("git")
type("appstore")
app_identifier(["com.drifthr.employee"])
# MATCH_PASSWORD comes from the env (see /Users/kp/drifthr-employee-MATCH_PASSWORD.txt, chmod 600, outside git).
